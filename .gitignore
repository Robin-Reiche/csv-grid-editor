node_modules/
out/
*.vsix
.vscode-test/
.vscode/
.claude/
.superpowers/
# Local test data: the big generated files and anything with real data in it.
# It lives next to the committed samples so there is one place to look, but it
# is far too large to commit (200 MB and up).
samples/local/
docs/
# media/ holds the committed static stylesheet (webview.css) AND build output.
# Ignore only the generated parts so CI ends up with a complete folder.
media/webview.js
media/codicon.css
media/codicon.ttf
media/ag-grid-community.min.js
media/ag-grid.css
media/ag-theme-alpine.css
feature_ideen.txt
test.csv
clearView_code_example
.omc/
# Raw screen recordings (huge). The optimized demo.gif / demo-2.gif are committed instead.
images/preview_gif1.gif
images/preview_gif2.gif